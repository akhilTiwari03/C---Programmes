#include <bits/stdc++.h>

using namespace std;

int main()
{
    string str = "(1,2),(2,3),(-2,1),(-3,4)";
    
    // char *ch = new char[str.size() +1];
    
    // strcpy(ch,str.c_str());
    // char *token = strtok(ch, "(,)");
    
    // while(token != NULL) {
    //     cout << token << endl;
    //     token = strtok(NULL, "(,)");
    // }

    stringstream ss(str);

    vector<int> ans;
    while(str.size()) {
        getline(ss,s,)
    }
    
    return 0;
}